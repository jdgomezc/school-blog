import {
  DB_HOST,
  DB_PORT,
  DB_NAME,
  DB_USER,
  DB_PASSWORD,
  BETTER_AUTH_SECRET,
  BETTER_AUTH_URL,
  IMG_FOLDER,
  PDF_FOLDER,
  APP_URL,
} from "astro:env/client";

export {
  DB_HOST,
  DB_PORT,
  DB_NAME,
  DB_USER,
  DB_PASSWORD,
  BETTER_AUTH_SECRET,
  BETTER_AUTH_URL,
  IMG_FOLDER,
  PDF_FOLDER,
  APP_URL,
};
